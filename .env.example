# CafeRoam Environment Variables
# Copy this file to .env.local and fill in the values.
# NEVER commit .env.local to git.

# ─── Supabase ──────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=           # Your Supabase project URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=      # Supabase anon/public key (safe to expose)
SUPABASE_SERVICE_ROLE_KEY=          # Supabase service role key (server-side only, never expose)
# JWT verification uses Supabase's JWKS endpoint ({SUPABASE_URL}/auth/v1/.well-known/jwks.json) — no secret needed here.

# ─── LLM Enrichment ────────────────────────────────────────────────────────────
LLM_PROVIDER=anthropic              # Options: anthropic | openai | gemini
ANTHROPIC_API_KEY=                  # Claude Haiku for taxonomy enrichment (default provider)

# ─── Embeddings ────────────────────────────────────────────────────────────────
EMBEDDINGS_PROVIDER=openai          # Options: openai | google
OPENAI_API_KEY=                     # OpenAI text-embedding-3-small (default provider)
GOOGLE_AI_API_KEY=                  # Google text-embedding-004 (fallback)

# ─── Email ─────────────────────────────────────────────────────────────────────
EMAIL_PROVIDER=resend               # Options: resend | sendgrid | postmark
RESEND_API_KEY=                     # Resend API key (default provider)
EMAIL_FROM=hello@caferoam.tw        # From address for all emails

# ─── Maps ──────────────────────────────────────────────────────────────────────
MAPS_PROVIDER=mapbox                # Options: mapbox | google
NEXT_PUBLIC_MAPBOX_TOKEN=           # Mapbox public token (safe to expose)

# ─── Analytics ─────────────────────────────────────────────────────────────────
ANALYTICS_PROVIDER=posthog          # Options: posthog | mixpanel | plausible
NEXT_PUBLIC_POSTHOG_KEY=            # PostHog project API key
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# ─── Error Tracking ────────────────────────────────────────────────────────────
NEXT_PUBLIC_SENTRY_DSN=             # Sentry DSN for frontend error tracking
SENTRY_DSN=                         # Sentry DSN for backend error tracking (same DSN or separate project)
SENTRY_AUTH_TOKEN=                  # Sentry auth token for source map uploads (CI only)
SENTRY_ORG=                         # Sentry organization slug
SENTRY_PROJECT=                     # Sentry project slug

# ─── Scraping ──────────────────────────────────────────────────────────────────
APIFY_API_TOKEN=                    # Apify API token for Google Maps scraping

# ─── Backend (for Next.js proxy routes) ────────────────────────────────────────
BACKEND_INTERNAL_URL=http://localhost:8000  # Railway internal: http://backend.railway.internal:8000

# ─── App ───────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000  # Production: https://caferoam.tw
NODE_ENV=development
